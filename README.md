# CodeRunner Lessons

This repository is a catalog of self-contained CodeRunner lesson projects.

Each module lives under `modules/` and is listed in `modules.json`. Plain Java
modules run from the editor Run button. Robot modules run from the CodeRunner
Driver Station.
